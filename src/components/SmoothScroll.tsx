"use client";

import { ReactNode } from "react";

// Lenis smooth scroll disabled — using native browser scroll
export default function SmoothScroll({ children }: { children: ReactNode }) {
    return <>{children}</>;
}
